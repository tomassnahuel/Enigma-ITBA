/**
 * Punto de entrada principal para la aplicación Mini Enigma CLI.
 * Permite la ejecución directa mediante: node index.js
 */

const { iniciarAplicacion } = require('./src/index.js');

iniciarAplicacion();
